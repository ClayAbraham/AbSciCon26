# Configurations

This directory contains reusable configuration templates.

- `config.cluster.example.yaml`: cluster-ready V1 pipeline template. Copy it to `config.cluster.yaml` and edit local paths before running.

Machine-specific config files such as `config.cluster.yaml` and `config.local.yaml` are ignored by `.gitignore`.
