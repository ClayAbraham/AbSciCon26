# Input Files

Place your compiled Excel workbook here.

Recommended filename:

- `metabolic_profiles.xlsx`

If you use a different name, update `workbook_path` in `config.example.yaml` or in your own config file.
